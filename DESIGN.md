# 天喜 TIANXI · DESIGN.md

品牌與表面規範。來源：`assets/tokens.css`（SSOT）。  
任何海報、公開站、監控台改版，先對呢份，再寫像素。

---

## 三套表面（唔好混用）

| 表面 | 目的 | 密度 | 出品 |
|---|---|---|---|
| 公開站 | Operate / Read：選馬、日程、戰績、引擎 | 手機欄 440、資料先行 | HTML |
| 海報卡 | 賽前預測、賽後比對；Telegram 一場一張 | 一屏講完、字少 | PDF（sendDocument） |
| 監控台 `/admin` `/ops` | 密度儀表：健康、日誌、開關 | 密、狀態色、等寬數字 | Worker HTML |

海報唔係網站截圖。網站唔係海報縮放。後台唔跟米紙留白。

---

## 物料（由 tokens 抽出）

紙／墨（海報同公開站淺色）：

- 紙 `--paper` `#FDFBF5`
- 紙 2 `#F5F1E4`　紙 3 `#EDE7D4`
- 墨 `--ink` `#0B0B0B`
- 墨軟 `#2A2A28`　墨啞 `#5F5C52`

品牌帶（頂欄，唔當主題）：

- `--ticker-bg` `#0B1612`
- `--ticker-fg` `#F0EBDD`
- 頂欄字用 `--tan` `#D4B78A`、點綴 `--gold-3` `#F5D77A`

語義色（只做強調，唔鋪滿底）：

- 金 `#B8912F` / `#D4A11E` — 命中、第一位、品牌線
- 綠 `#00843D` — 派彩、賠率跌（熱門）
- 紅 `#C8102E` — 賠率升（冷門）、未中
- 痞 `#5C3A1E` — 強調字

字體：

- 標題／馬名：Noto Serif TC / CJK HK Bold（`--font-display`）
- 正文：Noto Sans TC
- 馬號、排名、派彩、賠率：等寬 + `tabular-nums`（JetBrains Mono 或 Sans Bold tnum）
- 憲章：顯示字唔用 italic

對比：正文 ≥ 4.5:1；大字 ≥ 3:1。金只可做大字或深底上的小標，唔好金字配米紙做 11px 說明。

---

## 海報（Telegram PDF）

畫布：1080 × 1440，RGB，無出血。一場一頁。檔名：

- `TX-R{n}-pre.pdf`
- `TX-R{n}-post.pdf`

### 結構（兩張共用）

1. **頂帶** 1080×132，底 `#0B1612`。左細「喜」，中／左字 `TX-ORACLE天喜引擎 · 賽前預測` 或 `… · 預測與賽果比對`。唔居中堆三層標題。
2. **場次塊** 左對齊。日期＋場地一行墨啞；場次「第 n 場」用展示字，唔同標題搶大。
3. **主區** 食滿闊度。左右內邊 56。禁止四周幼框套住整張空白紙。
4. **底帶** `#0B1612` 或紙 3 實色條，放號碼粒或命中表。空白唔准超過畫布 22%。

### 賽前

- 四行：排名 1–4（保留）｜馬號大｜馬名展示字｜下一行細字：騎師 · 練馬師 · 檔 · 磨 · 跑法
- 第 1 位：金排名 + 左側 6px 金條。2–4 墨色。
- 唔寫％、唔寫彩池教學、唔寫「賽前預測卡」
- 底帶橫排四粒馬號（保留）

### 賽後

- 左欄「模型首四」、右欄「賽果頭四」，數字要大
- 兩邊都出現的號用金；只一邊出現用墨
- 寫「命中」，唔寫「重疊」
- 命中比數（如 3/4）放場次塊右側，唔好圓章插去兩欄中間
- 中咗先出表。每行：`命中{彩池}` ｜ `$10一注每注派彩` ｜ 綠色金額
- 未中的彩池唔出；唔解釋單T／三重彩

### 海報禁區（反通用 AI 套路）

- 四邊幼線框住一大塊米白
- 所有字水平垂直居中
- 中間大圓章／徽章搶主數字
- Inter / Roboto / Space Grotesk / 假金箔立體字
- 皇冠、獎盃、馬頭剪影、彩帶
- 彩虹漸變、玻璃擬態、強投影
- 一段免責聲明佔主區（免責最多 10px 墨啞，貼底帶內側）
- 網站 URL、`/cards/`、二維碼（除非產品明確要求）

---

## 公開站

跟現有 shell：頂欄深、底導航深、中間紙。最大欄 440。  
資料用 grid／row，數字 tnum。  
改版用 `frontend-design` + `impeccable` 收層級同對齊，唔好把選馬頁做成海報。

## 監控台

深底或高密度淺底二擇一，全頁統一。狀態：PASS 綠／WATCH 金／FAIL 紅。  
字級細、表密、等寬日誌。唔用海報頂帶當裝飾。

---

## 出品流程

1. 改視覺前讀本檔。  
2. 海報只經 `tools/tx_poster.py` 出 PNG + PDF。  
3. Telegram 用 `sendDocument` 傳 PDF，唔用 `sendMessage` 出卡。  
4. 公開站改 CSS／HTML；監控台改 Worker 模板。三套唔共用 layout 檔。
